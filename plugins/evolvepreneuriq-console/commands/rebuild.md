---
description: Rebuild the Evolvepreneur iQ console from the latest published template (keeps your existing data, just refreshes the shell)
---

Rebuild my Evolvepreneur iQ console. Fetch the newest template from the CDN, re-apply my CONFIG (owner, business, businesses list) and any custom Workflow/Skill cards I've added, but keep whatever Dashboard data is already in my current console rather than re-pulling it from EIQ. Validate, then deliver the HTML and update the `evolve-console` artifact in place.

Use the `evolvepreneuriq-console:console-generator` skill for this.
